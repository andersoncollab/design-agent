# Workflow Index — Skill Chains by Project Type

Use this to select the right skills for each project type. Skills are listed in recommended execution order.

## SaaS Web App
```
brand-research → color-system → typography-pairing → design-tokens
→ app-design-system → navigation-patterns → component-patterns → form-design
→ dashboard-patterns → error-states → onboarding-ux → dark-mode
→ accessibility-system → micro-interactions → design-qa → design-handoff
```

## Marketing / Landing Page Site
```
brand-research → aesthetic-direction → color-system → typography-pairing
→ landing-page-patterns → seo-design-patterns → image-optimization
→ scroll-animations → gsap-animations → micro-interactions
→ responsive-patterns → design-qa
```

## E-Commerce Site
```
brand-research → color-system → typography-pairing → navigation-patterns
→ landing-page-patterns → component-patterns → form-design → icon-systems
→ image-optimization → seo-design-patterns → responsive-patterns
→ micro-interactions → error-states → design-qa
```

## Mobile App (React Native / Flutter)
```
brand-research → color-system → typography-pairing → design-tokens
→ app-design-system → navigation-patterns → component-patterns → form-design
→ icon-systems → onboarding-ux → error-states → lottie-rive
→ micro-interactions → dark-mode → accessibility-system → design-handoff
```

## White-Label SaaS Platform
```
brand-research → design-tokens → white-label-systems → color-system
→ dark-mode → typography-pairing → component-patterns → navigation-patterns
→ form-design → dashboard-patterns → i18n-rtl → email-design
→ design-qa → design-handoff
```

## Agency Client Website
```
brand-research → competitive-design-intel → aesthetic-direction
→ color-system → typography-pairing → bento-layout → landing-page-patterns
→ seo-design-patterns → image-optimization → responsive-patterns
→ scroll-animations → micro-interactions → design-qa → client-deliverables
```

## Email Campaign
```
brand-research → color-system → typography-pairing → email-design
→ image-optimization → client-deliverables
```

## Social Media / Ad Creative
```
brand-research → color-system → typography-pairing
→ social-media-design → ad-creative-design → image-optimization
→ background-removal → client-deliverables
```

## Design System from Scratch
```
brand-research → aesthetic-direction → color-system → typography-pairing
→ design-tokens → design-system-generator → component-patterns → icon-systems
→ form-design → navigation-patterns → dark-mode → responsive-patterns
→ motion-design → micro-interactions → accessibility-system
→ figma-pipeline → design-handoff
```

## Animation-Heavy / Award Site
```
aesthetic-direction → color-system → gsap-animations → scroll-animations
→ page-transitions → lottie-rive → micro-interactions → motion-design
→ image-optimization → responsive-patterns → design-qa
```

## Figma-to-Code Pipeline
```
figma-pipeline → design-tokens → component-patterns → responsive-patterns
→ dark-mode → accessibility-system → design-to-code → design-qa
```

## Print / Brand Identity
```
brand-research → brand-identity-system → color-system → typography-pairing
→ print-design → client-deliverables
```

## Internationalized App
```
i18n-rtl → design-tokens → typography-pairing → color-system
→ component-patterns → form-design → navigation-patterns → icon-systems
→ responsive-patterns → accessibility-system → design-qa
```

## AI-Powered Product
```
agentic-ui → component-patterns → form-design → micro-interactions
→ error-states → onboarding-ux → dashboard-patterns → lottie-rive
→ accessibility-system → design-qa
```

---

## Quick Reference — Skill by Domain

| Domain | Core Skills |
|--------|------------|
| Brand | `brand-research`, `aesthetic-direction`, `brand-identity-system`, `competitive-design-intel` |
| Visual Foundation | `color-system`, `typography-pairing`, `design-tokens`, `dark-mode` |
| Layout | `bento-layout`, `responsive-patterns`, `landing-page-patterns`, `dashboard-patterns` |
| Components | `component-patterns`, `icon-systems`, `form-design`, `navigation-patterns` |
| Animation | `motion-design`, `scroll-animations`, `page-transitions`, `micro-interactions`, `gsap-animations`, `lottie-rive` |
| Content | `social-media-design`, `ad-creative-design`, `email-design`, `print-design` |
| Quality | `accessibility-system`, `design-qa`, `design-critique`, `visual-audit`, `image-optimization` |
| SEO | `seo-design-patterns`, `image-optimization` |
| Specialized | `i18n-rtl`, `white-label-systems`, `agentic-ui`, `onboarding-ux`, `error-states` |
| Workflow | `figma-pipeline`, `design-to-code`, `design-handoff`, `design-system-generator`, `client-deliverables` |
